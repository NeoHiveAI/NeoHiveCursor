---
name: generate-docs
description: DEPRECATED alias for `design-codebase-docs`. This slug will be removed in a future minor release. Use `design-codebase-docs` instead.
---

# Deprecated: `generate-docs`

This skill has been renamed to **`design-codebase-docs`** to better describe what the skill actually does — it designs a documentation standard and validates it with samples, rather than generating a full doc set. The behavior is identical.

Invoke the new skill now (`design-codebase-docs`) and stop.

Tell the user once: "Note: `generate-docs` was renamed to `design-codebase-docs`; please use the new name going forward."
