---
name: generate-post-submit-hook
description: DEPRECATED alias for `enable-smart-prompts`. This slug will be removed in a future minor release. Use `enable-smart-prompts` instead.
---

# Deprecated: `generate-post-submit-hook`

This skill has been renamed to **`enable-smart-prompts`** to remove internal implementation jargon (`post-submit-hook` was a Claude Code term). The behavior is identical.

Invoke the new skill now (`enable-smart-prompts`) and stop.

Tell the user once: "Note: `generate-post-submit-hook` was renamed to `enable-smart-prompts`; please use the new name going forward."
