---
name: revise-vector-memory
description: DEPRECATED alias for `capture-session-learnings`. This slug will be removed in a future minor release. Use `capture-session-learnings` instead.
---

# Deprecated: `revise-vector-memory`

This skill has been renamed to **`capture-session-learnings`** for end-user clarity (the old name described an internal mechanism rather than the user-facing outcome). The behavior is identical.

Invoke the new skill now (`capture-session-learnings`) and stop.

Tell the user once: "Note: `revise-vector-memory` was renamed to `capture-session-learnings`; please use the new name going forward."
